// Task 1
const isAuth = false;
const viewMode = isAuth ? "authMode" : "unauth";
console.log(viewMode);

// Task 2

const number = 13;
//diap od 6 do 12
const inOrOutDiap = number < 6 || number > 12 ? "out" : "in";
console.log(inOrOutDiap);
